process {

    executor = 'local'

    withName: alignment {
        cpus = 4
        memory = '8 GB'
    }

    withName: sort_and_index {
        cpus = 2
        memory = '4 GB'
    }
}

params {

    outdir = "results"
}